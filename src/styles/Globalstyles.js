import { NavLink } from "react-router-dom";

export const GlobalStyles = {
    navbar: {
        padding:10,
        justifyContent:"space-between",
        margin:20,
        
    },
    navlink: {
        padding:10,
    }
}